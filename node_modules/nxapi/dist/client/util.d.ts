import { NintendoAccountSession } from './storage/index.js';
export declare function checkUseLimit(session: NintendoAccountSession<unknown>, key: string, 
/** Set to false to count the attempt but ignore the limit */ ratelimit?: boolean, 
/** [requests, period_ms] */ limits?: [number, number]): Promise<void>;

import { Storage } from './storage/index.js';
interface UserConstructor<T extends User, A extends any[] = unknown[]> {
    createWithUserStore(users: Users, id: string, ...args: A): Promise<T>;
}
interface User {
    expires_at: number;
}
export default class Users {
    readonly storage: Storage;
    readonly znc_proxy_url?: string | undefined;
    private users;
    private user_promise;
    constructor(storage: Storage, znc_proxy_url?: string | undefined);
    get<T extends User, A extends any[]>(type: UserConstructor<T, A>, id: string, ...args: A): Promise<T>;
}
export {};

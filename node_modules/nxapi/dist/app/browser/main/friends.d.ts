import React from 'react';
import { User } from '../util.js';
import { Friend } from '../../../api/coral-types.js';
export default function Friends(props: {
    user: User;
    friends: Friend[];
    loading?: boolean;
    error?: Error;
}): React.JSX.Element;
declare function Friend(props: {
    friend: Friend;
    user?: User;
}): React.JSX.Element;
export {};

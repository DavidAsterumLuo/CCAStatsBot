import persist from 'node-persist';
import SplatNet3Api, { SplatNet3AuthData } from '../../api/splatnet3.js';
export interface SavedBulletToken extends SplatNet3AuthData {
}
export declare function getBulletToken(storage: persist.LocalStorage, token: string, proxy_url?: string, allow_fetch_token?: boolean, ratelimit?: boolean): Promise<{
    splatnet: SplatNet3Api;
    data: SavedBulletToken;
}>;

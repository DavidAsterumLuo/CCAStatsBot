import * as persist from 'node-persist';
import { MoonAuthData } from '../../api/moon.js';
import MoonApi from '../../api/moon.js';
export interface SavedMoonToken extends MoonAuthData {
    expires_at: number;
}
export declare function getPctlToken(storage: persist.LocalStorage, token: string, ratelimit?: boolean): Promise<{
    moon: MoonApi;
    data: SavedMoonToken;
}>;
